import styled from 'styled-components';
import { TableFooter } from '@material-ui/core';

export const TF = styled(TableFooter)`
  display: flex;
  justify-content: space-between;
  padding-right: 12%;
`
